@{
    ViewData["Title"] = "Tạo đề tài mới";
}

<div class="container">
    <div class="row justify-content-center" style="margin-top: 50px; margin-bottom: 50px;">
        <div class="col-md-8">
            <div class="card">
                <div class="card-header" style="text-align: center;">
                    <h4 class="mb-0">Đăng ký đề tài mới</h4>
                </div>
                <div class="card-body">
                    @if (TempData["Success"] != null)
                    {
                        <div class="alert alert-success alert-dismissible fade show">
                            @TempData["Success"]
                            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                        </div>
                    }
                    @if (TempData["Error"] != null)
                    {
                        <div class="alert alert-danger alert-dismissible fade show">
                            @TempData["Error"]
                            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                        </div>
                    }

                    <form method="post" asp-action="CreateProject">
                        <div class="mb-3">
                            <label for="Title" class="form-label">Tên đề tài <span class="text-danger">*</span></label>
                            <input type="text" class="form-control" id="Title" name="Title" required 
                                   placeholder="Nhập tên đề tài của bạn">
                        </div>

                        <!-- ✅ THÊM PHẦN THÀNH VIÊN NHÓM -->
                        <div class="card mb-3 border-info">
                            <div class="card-header bg-info bg-opacity-10">
                                <h6 class="mb-0">
                                    <i class="bi bi-people"></i> Thông tin thành viên nhóm (nếu có)
                                </h6>
                            </div>
                            <div class="card-body">
                                <div class="alert alert-info">
                                    <small>
                                        <i class="bi bi-info-circle"></i>
                                        <strong>Lưu ý:</strong> Nếu đề tài làm nhóm, vui lòng nhập thông tin các thành viên. Nếu làm cá nhân, có thể bỏ qua phần này.
                                    </small>
                                </div>

                                <div class="row mb-3">
                                    <div class="col-md-6">
                                        <label for="Member2Name" class="form-label">
                                            <i class="bi bi-person"></i> Họ tên thành viên 2
                                        </label>
                                        <input type="text"
                                               class="form-control"
                                               id="Member2Name"
                                               name="Member2Name"
                                               maxlength="100"
                                               placeholder="Nhập họ tên đầy đủ">
                                    </div>
                                    <div class="col-md-6">
                                        <label for="Member2StudentCode" class="form-label">
                                            <i class="bi bi-card-text"></i> Mã sinh viên thành viên 2
                                        </label>
                                        <input type="text"
                                               class="form-control"
                                               id="Member2StudentCode"
                                               name="Member2StudentCode"
                                               maxlength="50"
                                               placeholder="Ví dụ: 106221002">
                                    </div>
                                </div>

                                <div class="row">
                                    <div class="col-md-6">
                                        <label for="Member3Name" class="form-label">
                                            <i class="bi bi-person"></i> Họ tên thành viên 3
                                        </label>
                                        <input type="text"
                                               class="form-control"
                                               id="Member3Name"
                                               name="Member3Name"
                                               maxlength="100"
                                               placeholder="Nhập họ tên đầy đủ">
                                    </div>
                                    <div class="col-md-6">
                                        <label for="Member3StudentCode" class="form-label">
                                            <i class="bi bi-card-text"></i> Mã sinh viên thành viên 3
                                        </label>
                                        <input type="text"
                                               class="form-control"
                                               id="Member3StudentCode"
                                               name="Member3StudentCode"
                                               maxlength="50"
                                               placeholder="Ví dụ: 106221003">
                                    </div>
                                </div>
                            </div>
                        </div>

                        <div class="mb-3">
                            <label for="Description" class="form-label">Mô tả đề tài <span class="text-danger">*</span></label>
                            <textarea class="form-control" id="Description" name="Description" rows="6" required
                                      placeholder="Mô tả chi tiết về đề tài: mục tiêu, phạm vi, công nghệ sử dụng..."></textarea>
                        </div>

                        <div class="mb-3">
                            <label for="ProfessorID" class="form-label">Giảng viên hướng dẫn (tùy chọn)</label>
                            <select class="form-select" id="ProfessorID" name="ProfessorID">
                                <option value="">-- Chọn giảng viên --</option>
                                @foreach (var prof in ViewBag.Professors)
                                {
                                    <option value="@prof.UserID">@prof.FullName</option>
                                }
                            </select>
                            <small class="form-text text-muted">Bạn có thể để trống, admin sẽ phân công sau</small>
                        </div>

                        <div class="mb-3">
                            <label for="StartDate" class="form-label">Ngày bắt đầu dự kiến</label>
                            <input type="date" class="form-control" id="StartDate" name="StartDate" 
                                   value="@DateTime.Now.ToString("yyyy-MM-dd")">
                        </div>

                        <div class="alert alert-info">
                            <strong>Lưu ý:</strong>
                            <ul class="mb-0">
                                <li>Đề tài cần được giảng viên và admin phê duyệt trước khi thực hiện</li>
                                <li>Hãy mô tả rõ ràng và chi tiết về đề tài của bạn</li>
                                <li>Sau khi tạo và được xét duyệt, bạn có thể cập nhật tiến độ và upload tài liệu</li>
                            </ul>
                        </div>

                        <div class="d-flex justify-content-between">
                            <a href="@Url.Action("Dashboard", "Student")" class="btn btn-secondary">
                                <i class="bi bi-arrow-left"></i> Quay lại
                            </a>
                            <button type="submit" class="btn btn-primary">
                                <i class="bi bi-check-circle"></i> Đăng ký đề tài
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    .card {
        box-shadow: 0 2px 8px rgba(0,0,0,0.1);
        border-radius: 10px;
    }
    .form-label {
        font-weight: 500;
    }
</style>